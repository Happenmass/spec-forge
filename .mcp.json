{
  "mcpServers": {
    "session-ledger": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/session-ledger/server.mjs"],
      "env": {
        "SESSION_LEDGER_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
